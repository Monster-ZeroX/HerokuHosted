#!/bin/bash

# Heroku pre-build script to install system dependencies

echo "-----> Installing system dependencies"

# Install rclone
curl https://rclone.org/install.sh | bash

echo "-----> System dependencies installed"
